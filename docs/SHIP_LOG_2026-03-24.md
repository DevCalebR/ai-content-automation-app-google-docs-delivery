# Production Promotion Record — 2026-03-24

## Summary

Production was serving pre-fix submit behavior because the working runtime fix existed only on `feat/google-oauth-delivery-readiness`, while `main` was the production-driving branch. The approved fix was promoted by cherry-picking two scoped commits onto `main`, pushing `main`, and verifying the public Vercel deployment at [https://ai-content-automation-app-google-do.vercel.app](https://ai-content-automation-app-google-do.vercel.app).

## Root Cause

- Production drift / branch drift
- `main` drove production
- The working runtime fix was not on `main`

## Promotion Method

1. Created a clean temporary worktree from `main`
2. Reset that worktree to `origin/main`
3. Cherry-picked only these approved source commits:
   - `931566b42dc8606cd4423449d042e2849a3c6ed4`
   - `cf74d8c0136f128d08977c953b89910492ce70c0`
4. Pushed `main`
5. Removed the temporary worktree
6. Left the dirty OAuth worktree on `feat/google-oauth-delivery-readiness` untouched

## Validation

Validation was run in the clean temporary `main` worktree before push:

- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run build`

All passed.

## Production Verification

Public app verified at [https://ai-content-automation-app-google-do.vercel.app](https://ai-content-automation-app-google-do.vercel.app).

Sign-up flow:

- Invalid sign-up stayed on `/sign-up`
- Inline validation errors were shown
- Entered values remained in place after failed submission
- Valid sign-up redirected to:
  - `/sign-in?registered=1&email=qa%2Bprod-signup-1742790855%40example.com`

Generation flow:

- `Generate structured plan` changed to `Generating plan...`
- The request completed and redirected to results
- Run status was `SUCCEEDED`
- Verified results URL:
  - `/app/workspaces/cmn40804b0008qpn37foa1v5x/results/cmn43rrqa0001l504zrd019uj`

## Files Included

The following files were promoted via the two cherry-picked commits:

- `app/(auth)/actions.ts`
- `components/auth/sign-up-form.tsx`
- `components/ui/submit-button.tsx`
- `lib/auth/register.ts`
- `lib/validations/auth.ts`
- `tests/auth/register-user-action.test.ts`
- `tests/ui/submit-button.test.tsx`
- `tests/auth/auth-options.test.ts`
- `tests/auth/register-user.test.ts`

## Commit References

- Source branch: `feat/google-oauth-delivery-readiness`
- Source commits:
  - `931566b42dc8606cd4423449d042e2849a3c6ed4` — `fix(forms): restore signup and generation submissions`
  - `cf74d8c0136f128d08977c953b89910492ce70c0` — `test(auth): harden signup and session regression coverage`
- Cherry-picked commits on `main`:
  - `9ae754cc1df5e4c1b9736d67bb63433d1f352a89` — `fix(forms): restore signup and generation submissions`
  - `3d3a1ff6e8c2c0c462d880fb80b2a071cedf78c5` — `test(auth): harden signup and session regression coverage`
- Vercel production deploy completed successfully for:
  - `3d3a1ff6e8c2c0c462d880fb80b2a071cedf78c5`

## Notes

- Unrelated local OAuth work on `feat/google-oauth-delivery-readiness` was preserved and not staged, merged, or pushed.
